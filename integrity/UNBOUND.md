# Unbound matrix rows

85 rows, 47 bound, 38 unbound. Status SSOT: [`binding-matrix.json`](binding-matrix.json).

## Keep unbound

Classification: R1 R2 R3 R4 R16 C1 C2 C3 C13 C21 C22 C23 C24.
Mutation: R6 C5 R8 — verb-path v1 exists, cousin only.
Contracts: R9–R13 C7–C11.
Composition: R15 R17 R18 C14 C15.
Integrity: P2 R20 R25 C16 C17 C18 C19.

A5 is an audit concern with a v1 tool (`fitness-taint-lifetime.py`). It is **not** a matrix id. Do not add a decorative row to hide that.

## R24 binder

Must be `tools/fitness-adjective-locality.py`. If JSON still says field-writes, flip that one field.
