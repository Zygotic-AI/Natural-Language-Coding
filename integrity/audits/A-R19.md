# A-R19

- Requirement: `R19`
- Outcome: **met** | **not met** only
- Gate: `tools/fitness-no-hand-authored-graphs.py`

## Statement

Do not maintain a separate hand-authored execution-graph file that duplicates a durable goal's runtime definition.

## Binary criteria

Met iff the registered gate exits 0 with `RESULT:MET` on the scanned tree. Not met if the gate fails, did not run, or evidence is missing.

## Evidence

On met or not met, cite file and symbol (or N/A reason). List failing ids when the audit is a matrix audit.
