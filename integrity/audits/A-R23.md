# A-R23

- Requirement: `R23`
- Outcome: **met** | **not met** only
- Gate: `tools/fitness-no-noun-field-writes.py`

## Statement

A gate fails the change if a goal or adapter assigns a noun field or imports a noun internals module.

## Binary criteria

Met iff the registered gate exits 0 with `RESULT:MET` on the scanned tree. Not met if the gate prints `VIOLATION` / exits 1, did not run, or evidence is missing.

Field-assignment half is covered by `fitness-no-noun-field-writes.py`. Import-of-internals half is a sibling concern of R6/C5 and `fitness-noun-imports.py` where that tool exists.

## Evidence

Cite each `VIOLATION` line, or `RESULT:MET` with the scan roots used.
