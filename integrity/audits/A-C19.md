# A-C19

- Requirement: `C19`
- Outcome: **met** | **not met** only

## Statement

No second implementation of the same adjective exists in the diff (search for duplicated predicates).

## V1 tool (not a matrix bind)

`tools/fitness-duplicated-adjective.py` — inside one `domain/<noun>/`, a token from
`adjectives.txt` may appear in only one source file.

Designed fail: `examples/duplicated-adjective-violation/` (`void` in invoice.py and rules.py).
Designed pass: that tree on R24 (the leak is not outside the noun).

V1 is not diff-scoped. C19 stays unbound.
