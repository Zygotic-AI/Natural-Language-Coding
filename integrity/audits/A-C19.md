# A-C19

- Requirement: `C19`
- Outcome: **met** | **not met** only
- Gate: `tools/fitness-duplicated-adjective.py`

## Statement

No second implementation of the same adjective exists in the diff (search for duplicated predicates).

## V1 bind

Tree-wide, not diff-scoped: inside one `domain/<noun>/`, a token from `adjectives.txt` may appear in only one **implementation** file. `tests/` and `test_*.py` are skipped — tests are allowed to name adjectives (see C16).


Designed fail: `examples/duplicated-adjective-violation/`.
Designed pass: `examples/invoice-correct/`.
