# A-R13

- Requirement: `R13`
- Outcome: **met** | **not met** only
- Gate: `tools/fitness-r13-entrypoints.py`

## Statement

One public entrypoint per goal.

## V1 bind

A `goals/<id>/` directory with two or more files named `implementation|run|main|index|handler|entry|public` plus a source suffix fails.

Does not parse `export function`. Two doors as two files.

Designed fail: `examples/extra-goal-entrypoint/` (`implementation.py` and `run.py`).
Designed pass: `examples/invoice-correct/`.

C11 stays unbound: it is "each *changed* goal" (diff-scoped).
