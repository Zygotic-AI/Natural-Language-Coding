# A-R33

- Requirement: `R33`
- Outcome: **met** | **not met** only
- Gate: `tools/fitness-escape-hatch.py`

## Statement

Noun state is not mutated through non-OO escape hatches from outside a published verb.

## V1 bind

`__dict__`, `vars(`, `exec(`, `eval(` in goals/adapters.

Raw SQL / `.save(` / `setattr(` stay on R6. Named field assignment stays on R5.

Designed fail: `examples/invoice-escape-hatch-violation/`.
Designed pass: `examples/invoice-correct/`.

Was adversarial A7. Same tool.
