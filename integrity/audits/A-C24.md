# A-C24

- Requirement: `C24`
- Outcome: **met** | **not met** only
- Gate: `tools/fitness-c24.py`

## Statement

Ratification is recorded for classes that require it.

## Bind

Class A/B/D/E/F must have `Ratified-by: <name>`. Empty or an agent/AI/bot/confirmer
name is `agent-ratified`. Class C skips.

Compile (`ci-fitness.sh`) can MET on class C with no human. Ship (`tools/release-audit.py`) cannot: it requires `Released-by:` a human on CONFIRM.md in addition to C24 for A/B/D/E/F.


Designed fail: `examples/unsigned-class-a/`, `examples/agent-ratified/`.
Designed pass: `examples/invoice-correct/` (class C).
