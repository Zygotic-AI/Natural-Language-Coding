# A-C10

- Requirement: `C10`
- Outcome: **met** | **not met** only
- Gate: `tools/fitness-c10.py`

## V1 bind

Schema `version` other than 1 / "1" requires an `adrs/*.md` under the scan
root. Version 1 → skip.

Does not detect that a change was breaking. Hub ADRs do not count for an
example tree.

Designed fail: `examples/version-bump-no-adr/`.
Designed pass: `examples/invoice-correct/` (version 1).
