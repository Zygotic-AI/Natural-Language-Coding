# A-R25

- Requirement: `R25`
- Outcome: **met** | **not met** only
- Gate: `tools/fitness-r25-noun-tests.py`

## Statement

Tests for a noun's adjectives live next to the noun and run on every verb. Goal tests do not replace them.

## V1 bind

A `domain/<noun>/` with `fields.txt` or `adjectives.txt` must have `tests/*.py` (or `test_*.py` / `*_test.py` in the noun dir).

Does not run the tests. Does not check they name adjectives. C16–C18 stay unbound.

Designed fail: `examples/noun-without-tests/`.
Designed pass: `examples/invoice-correct/`.
