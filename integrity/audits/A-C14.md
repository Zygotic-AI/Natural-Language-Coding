# A-C14

- Requirement: `C14`
- Outcome: **met** | **not met** only
- Gate: `tools/fitness-goal-imports.py`

## Statement

Goal-to-goal calls use public entrypoints only; no imports of another goal's internals.

Same binder as C12 / R14. V1 is import-of-internals, not "called the public function by the wrong name."
