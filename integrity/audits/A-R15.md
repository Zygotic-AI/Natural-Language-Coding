# A-R15

- Requirement: `R15`
- Outcome: **met** | **not met** only
- Gate: `tools/fitness-r15-copied-helpers.py`

## Statement

Shared domain logic that protects a noun lives on the noun, not in a helper copied into two goals.

## V1 bind

A Python function of 3+ non-empty, non-comment lines whose normalized body
appears under two `goals/<id>/` directories fails.

One-liners (a single verb call) are ignored.

Does not parse imports of a shared module — copied *source* only.

Designed fail: `examples/copied-goal-helper/`.
Designed pass: `examples/invoice-correct/`.
