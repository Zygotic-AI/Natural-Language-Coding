# A-C18

- Requirement: `C18`
- Outcome: **met** | **not met** only
- Gate: `tools/fitness-c18.py`

## Statement

Goal tests cover the use-case, not a copy of the noun's adjective suite.

## V1 bind

Goal test files (`goals/**/tests/`, `test_*.py` under a goal) must not mention a
token from `domain/<noun>/adjectives.txt`.

No goal tests → skip (MET). That is not proof a use-case test exists.

Designed fail: `examples/goal-tests-copy-adjectives/`.
Designed pass: `examples/invoice-correct/` (no goal tests).
