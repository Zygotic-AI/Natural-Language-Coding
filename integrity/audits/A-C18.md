# A-C18

- Requirement: `C18`
- Outcome: **met** | **not met** only
- Gate: `tools/fitness-c18.py`

## Statement

Goal tests cover the use-case, not a copy of the noun's adjective suite.

## V2 bind

A goal with implementation `.py` must have tests. Those tests must not mention
tokens from `domain/<noun>/adjectives.txt`.

Does not prove the test actually invokes the goal (name of file is enough
if it lives under `tests/`).

Designed fail: `examples/goal-untested/` (no tests),
`examples/goal-tests-copy-adjectives/` (copied `paid`).
Designed pass: `examples/invoice-correct/`.
