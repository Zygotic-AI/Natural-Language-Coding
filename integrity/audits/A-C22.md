# A-C22

- Requirement: `C22`
- Outcome: **met** | **not met** only
- Gate: `tools/fitness-c22.py`

## Statement

Charter/ADR/code/contracts were updated in the same change if they were affected.

## Bind

Note must record `C22 — PASS|N/A`.
N/A is a violation when the **change set** (`CHANGED:` list, else dirty git,
else `origin/main...HEAD`) includes charter/adrs *and* goals/domain/schema.
PASS must cite those sides.

No change set → N/A is allowed (nothing in this change to lie about).

Designed fail: `examples/missing-c22/`, `examples/c22-na-with-both/`.
Designed pass: `examples/invoice-correct/`.
