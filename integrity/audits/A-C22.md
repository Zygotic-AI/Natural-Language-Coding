# A-C22

- Requirement: `C22`
- Outcome: **met** | **not met** only
- Gate: `tools/fitness-c22.py`

## Statement

Charter/ADR/code/contracts were updated in the same change if they were affected.

## V2 bind

Note must record `C22 — PASS|N/A`.
N/A is a violation when this tree has `CHARTER.md` or `adrs/` *and* `goals/` or `domain/` source.
PASS must cite an existing charter/adr path and an existing code path.

Does not inspect git. Tree shape, not the diff.

Designed fail: `examples/missing-c22/`, `examples/c22-na-with-both/`.
Designed pass: `examples/invoice-correct/` (no charter in the fixture tree).
