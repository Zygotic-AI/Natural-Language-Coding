# A-C22

- Requirement: `C22`
- Outcome: **met** | **not met** only
- Gate: `tools/fitness-c22.py`

## Statement

Charter/ADR/code/contracts were updated in the same change if they were affected.

## V1 bind

If `PROPOSAL.md` / `CONFIRM.md` exists, it must contain `C22 — PASS` or `C22 — N/A`.

No note → skip. Does not inspect git. Label presence, not that the files actually moved together.

Designed fail: `examples/missing-c22/`.
Designed pass: `examples/invoice-correct/`.
