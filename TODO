# TODO — Boundary-Based Programming

SSOT for open work. Charter SSOT is `CHARTER.md`.

## How to read red vs green

Designed red: matching tool on its specimen → NOT_MET.
Assert-fails scripts → exit 0 ASSERT:PASS.
Designed pass: invoice-correct MET; assert-impact-graph-generated.py ASSERT:PASS.
True miss: invoice-correct red, assert-fails exit 1, or a specimen green on its own tool.

---

## Completed product slice

In-reach v1/v2 gates, hub CI, release-audit, matrix bound. Log: git history.
HOLES.md names what a static gate cannot do.

## Open — not now

SSOT: [`HOLES.md`](HOLES.md) (judgment / human). Next implementation is **not** more Python gates.

## Backlog

- **Language scanner** — parked. Spec first, then a thin adapter. Do not fork `fitness-*.py` per language. SSOT: [`docs/LANGUAGE-SCANNER.md`](docs/LANGUAGE-SCANNER.md).
- **Rule IR** — parked. ADR 0007 is accepted; no `R` id until a bindable if-then gate exists. Tags/primitives/rules are charter, not v1 Python regex.
