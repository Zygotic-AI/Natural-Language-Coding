# Charter corpus pointer (E1)

Until the two-line insert lands in `CHARTER.md` §3, this file is the pointer.

Corpus tags for every published **R** / **C** / **P** id live in [`integrity/rule-corpus.json`](../../integrity/rule-corpus.json) ([ADR 0024](../../adrs/0024-nlc-factory-spine.md)):

- `bba` — emit shape
- `nlc` — factory process

Charter body insert (paste after the R/C paragraph in §3):

> Corpus tags for every published **R** / **C** / **P** id live in `integrity/rule-corpus.json` (ADR 0024): `bba` = emit shape, `nlc` = factory process.
