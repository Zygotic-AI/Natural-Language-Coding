[Specimen](../../docs/TERMS.md#specimen). Adopter tree with rules, markers, before-generate stamp, and gate records — **`verify_fast_blockers` + `pipeline_blockers` MET** (ADR 0021/0023). Fingerprints: `python3 ../../tools/nlc.py --project . verify-deep` (records `.nlc/verified.json`). **contract-change:** exempt (specimen). To opt into ADR 0006 contract fitness on a specimen, add the opt-in line documented in `product_tree.CONTRACT_OPT_IN`.
