# schema-identity-violation

Specimen for **R11 / C9 v1**, not a product.

`amount` is an integer on the goal input and a string on the noun verb.
Same name, two types.

Designed fail: `tools/fitness-schema-identity.py` → NOT_MET.
