# missing-failure-mode

Specimen for **P4 / R31 v1** (code boundaries), not a product.

Input and output schemas exist. Failure mode does not. That is R9-green and P4-red.

Designed fail: `tools/fitness-boundary-io.py` → NOT_MET.
Designed pass on this tree: contract-presence (R9).
