# duplicated-adjective-violation

Specimen for **C19 v1**, not a product.

`void` is implemented in `invoice.py` and again in `rules.py` inside the same noun.
That is a second implementation, not an outside-noun leak (R24).

Designed fail: `tools/fitness-duplicated-adjective.py` → NOT_MET.
R24 on this tree must be MET.
