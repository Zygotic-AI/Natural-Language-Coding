# UC20 reference — TypeScript domain call-tree pack

Per-stack reference beyond Python-only `domain/*.py`. Inventory synced in `.nlc/verb-primitives.json`.
Run: `python3 tools/nlc_call_tree.py --check --root examples/call-tree-pack-reference`
